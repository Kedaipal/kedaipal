node_modules
.DS_Store

# Claude Code session worktrees (each is a full repo copy)
.claude/worktrees/
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
src/paraglide/

# graphify knowledge graph build artifacts (regenerable via /graphify)
graphify-out/

# only for individual dev usage
.dev.vars